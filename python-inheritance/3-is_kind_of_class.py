#!/usr/bin/python3
"""Definition to check if an object is an instance of the class"""


def is_kind_of_class(obj, a_class):
    """checks if the object is an instance from a_class"""
    return isinstance(obj, a_class)
